## 逐行输入

```cpp
#include <iostream>
#include <sstream>
#include <string>
#include <vector>
using namespace std;
int main() {
    string line;
    vector<int> numbers;
    // 读取每一行
    while (getline(cin, line)) {
        if (line.empty()) continue;
        // 使用字符串流解析当前行
        istringstream iss(line);
        int num;
        numbers.clear();
    return 0;
} 
```